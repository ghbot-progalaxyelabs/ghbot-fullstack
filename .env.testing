# Testing Environment Configuration
# This file is used for E2E testing with Playwright

# Database Configuration (Test Database)
DB_NAME=webmeteor_test
DB_USER=webmeteor
DB_PASSWORD=webmeteor_test_password
DB_HOST=localhost
DB_PORT=5433

# API Configuration
API_URL=http://localhost:8000
API_PORT=8000

# Frontend Configuration
WWW_URL=http://localhost:4200
WWW_PORT=4200

# Application Environment
APP_ENV=testing
MOCK_AUTH_ENABLED=true

# JWT Secret (for testing only - DO NOT use in production)
JWT_SECRET=test-jwt-secret-key-for-testing-only-change-in-production

# GitHub Webhook Secret (for testing)
GITHUB_WEBHOOK_SECRET=test-webhook-secret

# ZeptoMail (optional for testing, can be mocked)
ZEPTOMAIL_BOUNCE_ADDRESS=bounce@test.example.com
ZEPTOMAIL_SENDER_EMAIL=noreply@test.example.com
ZEPTOMAIL_SENDER_NAME=Test WebMeteor
ZEPTOMAIL_SEND_MAIL_TOKEN=test-token

# Google OAuth (mock values for testing)
GOOGLE_CLIENT_ID=mock-google-client-id
